name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: |
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Build APKs
        shell: bash
        run: |
          set +e
          gradle :app:assembleDebug :app:assembleRelease --stacktrace 2>&1 | tee build.log
          status=${PIPESTATUS[0]}
          if [ "$status" -ne 0 ]; then
            grep -E -i "(^e: |error:|FAILURE:|What went wrong|Execution failed|Could not|Unresolved reference|Cannot access|A failure occurred)" build.log > build-error-lines.txt || true
            {
              echo "Gradle build failed. Last 200 log lines:"
              tail -n 200 build.log
            } > build-error-summary.txt
            if [ -s build-error-lines.txt ]; then
              echo "::error file=.github/workflows/android-debug-apk.yml,line=1::$(cat build-error-lines.txt | head -n 30 | tr '\n' ' ' | sed 's/%/%25/g; s/\r/%0D/g; s/:/%3A/g')"
            else
              echo "::error file=.github/workflows/android-debug-apk.yml,line=1::$(tail -n 80 build.log | tr '\n' ' ' | sed 's/%/%25/g; s/\r/%0D/g; s/:/%3A/g')"
            fi
            exit "$status"
          fi

      - name: Prepare APK files
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/app-debug.apk dist/shorts-auto-debug.apk
          cp app/build/outputs/apk/release/app-release.apk dist/shorts-auto-release.apk

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: shorts-auto-apks
          path: dist/*.apk

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-build-log
          path: |
            build.log
            build-error-summary.txt
            build-error-lines.txt

      - name: Publish latest APK release
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete apk-latest --yes || true
          gh release create apk-latest dist/shorts-auto-release.apk dist/shorts-auto-debug.apk \
            --title "Latest APK" \
            --notes "최신 APK입니다. 먼저 shorts-auto-release.apk를 설치해 보세요. 막히면 shorts-auto-debug.apk도 제공됩니다." \
            --prerelease \
            --latest
